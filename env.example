# Variables de Entorno para Ventas Cenáculo
# Copia este archivo a .env y configura los valores según tu entorno

# Puerto del servidor
PORT=3000

# Host del servidor (0.0.0.0 para escuchar en todas las interfaces)
HOST=0.0.0.0

# Entorno de ejecución
NODE_ENV=production

# ========== BASE DE DATOS PostgreSQL ==========
# Render automáticamente expone: PGHOST, PGPORT, PGUSER, PGPASSWORD, PGDATABASE
# El código las detecta automáticamente. Si prefieres nombres personalizados:
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=CenaculoDB
DB_SSL=false
# Para Render, usa DB_SSL=true si la base de datos requiere SSL

# ========== SUPABASE STORAGE (Para imágenes de comprobantes) ==========
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key
SUPABASE_BUCKET=comprobantes

# ========== CONFIGURACIÓN DE NEGOCIO ==========
NOMBRE_NEGOCIO=Cenáculo
MONEDA=ARS
TIMEZONE=America/Argentina/Buenos_Aires

# ========== SEGURIDAD ==========
PALABRA_CLAVE_CIERRE=GraciasSanJose

# ========== CONFIGURACIÓN DE VASOS ==========
VASOS_POR_BOTELLA=4

# Nota: No incluyas información sensible en este archivo
# Las variables de entorno se configuran en Railway o tu plataforma de despliegue

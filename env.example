# Variables de Entorno para Ventas Cenáculo
# Copia este archivo a .env y configura los valores según tu entorno

# Puerto del servidor (Railway, Render, etc. asignan automáticamente)
PORT=3000

# Host del servidor (0.0.0.0 para escuchar en todas las interfaces)
HOST=0.0.0.0

# Entorno de ejecución
NODE_ENV=production

# Base de datos SQLite (ruta relativa, se crea automáticamente)
# En producción, considera usar una base de datos externa
DB_PATH=./ventas.db

# Configuración de WebSocket
WS_PORT=3000

# Logging
LOG_LEVEL=info

# Railway (se configura automáticamente en Railway)
# RAILWAY_ENVIRONMENT=production

# Nota: No incluyas información sensible en este archivo
# Las variables de entorno se configuran en la plataforma de despliegue

